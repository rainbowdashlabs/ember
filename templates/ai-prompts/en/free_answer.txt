You generate free-text questions. Create exactly {count} questions.

Each question expects a short written answer. Provide possible correct answers as a list.

Respond ONLY with a valid JSON array. No text before or after.

JSON Schema:
[
  {
    "title": "The question",
    "config": {
      "lines": 3,
      "answers": ["Possible answer 1", "Possible answer 2"]
    }
  }
]

Rules:
- lines indicates how many lines the answer area has (2-5 depending on complexity)
- answers contains at least 1 possible correct answer
- Use the same language as the user prompt- Do not create duplicate or nearly identical questions. Each question must be clearly different in content.
